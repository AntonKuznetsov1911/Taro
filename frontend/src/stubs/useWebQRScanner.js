// Заглушка для expo-camera/build/web/useWebQRScanner.
//
// Оригинальный модуль на верхнем уровне создаёт Worker с
// importScripts('https://cdn.jsdelivr.net/npm/jsqr@.../jsQR.min.js') — и делает это
// при импорте, а не когда сканер включён. Приложение сканированием QR не пользуется
// (камера нужна только для снимка ладони), но экран камеры из-за этого лез в интернет
// и падал с ошибкой на устройстве без сети.
//
// Здесь хук ничего не делает: сигнатура сохранена, побочных эффектов нет.
export function useWebQRScanner() {
  // Сканер QR не поддерживается и не нужен — намеренно пустая реализация.
}

export default useWebQRScanner;
